@model FirstApp.Models.GetCarViewModel
@{
    ViewBag.Title = "Cars";
}

<h2>Cars</h2>

<ul>
    @foreach(var car in Model.Cars)
            {
                <li>@car.Id - @car.Name</li>
            }
</ul>
